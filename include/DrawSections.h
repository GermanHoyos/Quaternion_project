#include "MasterHeader.h"

class DrawSections
{
public:
   // All strings
   string eOpacityIn = "UI Animations";

   // Seperate ui Sections can be inluded here:
   void draw()
   {
      // DrawText(eOpacityIn.c_str(), 805, 315, 20, GREEN);
      // DrawRectangleLines(805, 340, 790, 650, GREEN);
   }
};
